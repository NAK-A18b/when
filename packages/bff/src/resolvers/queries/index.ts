import { users } from "./users";
import { centurias } from "./centurias";
import { connections } from "./connections";
import { currentUser } from "./currentUser";
import { delays } from "./delays";

export const Query = {
  users,
  centurias,
  connections,
  currentUser,
  delays
};
