{
  "name": "when-frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:client": "cd client && npm start",
    "dev:server": "serverless offline",
    "build:client": "cd client && npm run build",
    "deploy:client": "serverless client deploy",
    "deploy:server": "serverless deploy",
    "deploy": "npm-run-all --parallel deploy:client deploy:server",
    "dev": "npm-run-all --parallel dev:client dev:server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-finch": "^2.4.2",
    "serverless-offline": "^5.10.1",
    "npm-run-all": "^4.1.5"
  }
}
