{
  "name": "whatsapp-web-driver",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "invoke:app": "npm run build && node build/app/index.js",
    "invoke:update-timetable": "npm run build && sls invoke local --function updateTimetable",
    "build": "babel --presets es2015 -d build/ src",
    "start": "npm run build && npm run dev",
    "run-locally": "npm run build && npm run invoke:import",
    "deploy": "sls deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "selenium-webdriver": "4.0.0-alpha.4",
    "request": "2.88.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "request": "^2.88.0",
    "serverless-dynamodb-local": "^0.2.38",
    "serverless-offline": "^5.9.0"
  }
}
